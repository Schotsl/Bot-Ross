export interface Settings {
  youtubeAPI: string;
  todoistAPI: string;
  discordAPI: string;
  playlistId: string;
  discordId: string;
}

export interface Schema {
  name: string;
  enabled: boolean;
}
